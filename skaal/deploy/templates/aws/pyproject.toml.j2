# Generated by `skaal build` from skaal/deploy/templates/aws/pyproject.toml.j2.
# Edits to this file are overwritten on the next build.
[project]
name = "skaal-build-{{ resource_bare_name }}"
version = "0.0.0"
requires-python = ">={{ python_version }}"
dependencies = [
{%- for req in requirements %}
    "{{ req }}",
{%- endfor %}
]
