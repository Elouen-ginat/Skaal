# {name}

A Skaal app.

## Quickstart

```sh
pip install -e .
skaal run
```

The server hot-reloads on edits. Hit it:

```sh
curl -s -X POST localhost:8000/_skaal/invoke/increment -d '{{"name":"hits"}}'
curl -s -X POST localhost:8000/_skaal/invoke/get_count  -d '{{"name":"hits"}}'
```
